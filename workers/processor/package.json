{
  "name": "@podads/processor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types"
  },
  "dependencies": {
    "@podads/shared": "workspace:*",
    "mp3-parser": "^0.3.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.2"
  }
}
